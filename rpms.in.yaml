arches:
  - x86_64
  - aarch64
  - ppc64le
contentOrigin:
  repofiles:
    - ubi.repo
packages:
  - cmake
  - clang
  - llvm
  - openssl
  - ca-certificates
  - wget
context:
  containerfile: backend/Dockerfile.konflux.api
